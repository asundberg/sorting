// Split Function

// Merge sort uses a split function that can split an array into two separate arrays.

// The function signature would look like:

describe('Split Array function', function() {
  it('is able to split an array into two halves', function() {
    // your code here 
  });
});
// Consider what you would like to do for odd-length arrays.


// Merge Function

// Merge Sort uses a merge algorithm for combining sorted arrays into a single sorted array. Create a test for this function in mergesort.spec.js:

describe('Merge Sort', function(){
  it('is able to merge two sorted arrays into one sorted array', function(){
    // test the merging algorithm
  });
});

// mergeSort Function

// Now you have all the merge and sort functions, implement the recursive mergeSort function.

// The function signature could look like:

function mergeSort(array) {

  /* your code here */

}
// You should also write a test to test the mergeSort function.